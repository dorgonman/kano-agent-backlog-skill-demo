# Backlog Tools (project-specific)

Keep project-only scripts here (e.g., iteration views, last-N-days focus views).
Generic workflows should live in the skill and be exposed via `python skills/kano-agent-backlog-skill/scripts/kano <command>`.
