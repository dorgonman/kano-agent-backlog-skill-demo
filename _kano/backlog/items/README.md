# _kano/backlog/items

Store work items here, grouped by type:

- `epics/`
- `features/`
- `userstories/`
- `tasks/`
- `bugs/`

Hierarchy is defined by the `parent` field in frontmatter (not by folders).
